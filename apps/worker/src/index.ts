import 'dotenv/config';

/**
 * BullMQ Worker Entry Point
 * Processes background jobs: PDF generation, exports, email, cache invalidation.
 *
 * Phase 1: Placeholder — workers will be implemented as features mature.
 * For now, this file exists to maintain the monorepo structure.
 */

console.log('🔧 Worker started (no queues configured yet — Phase 2)');
console.log('   Available queues will include:');
console.log('   - report-pdf: Generate PDFs from report snapshots');
console.log('   - data-export: CSV/XLSX export');
console.log('   - email-dispatch: Report sharing notifications');
console.log('   - cache-invalidation: CDN purge on report revocation');
